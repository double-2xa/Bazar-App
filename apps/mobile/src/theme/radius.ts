export const radius = {
  xs: 8,
  sm: 12,
  md: 16,
  lg: 20,
  xl: 28,
  xxl: 36,
  full: 9999,
};

/** @deprecated Use `radius` — kept for backward compatibility */
export const borderRadius = radius;
